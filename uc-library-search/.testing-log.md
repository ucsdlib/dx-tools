# UC Library Search Skill — Testing Log

Behind-the-scenes record of validation and forward-testing for the `uc-library-search`
skill. This file is intentionally a hidden dotfile so standard agent file-discovery
(ripgrep's default hidden-file skipping) ignores it. It is **not** part of the skill's
operational content and is never referenced from `SKILL.md`.

## 1. Static validation (2026-09-15)

- `quick_validate.py` → `Skill is valid!` (frontmatter: name `uc-library-search`,
  description 912 chars, no angle brackets).
- `scripts/build_url.py --self-test` → `PASS (7 cases)` covering spaces, phrase quotes,
  parentheses, wildcard `?` → `%3F`, truncation `*`, field codes, and filters incl. date range.
- Grep checks: no stale absolute phrasing ("always asks 2-4", "even if the topic seems
  clear, ask 2–4"); needs-based questioning policy present in the description, Behavioral
  Constraint 3, Stage 1, Edge Cases, and QA checklist item 9.

## 2. Live URL verification (search-library.ucsd.edu)

All formats exercised in the browser against the production instance:

| Check | Result |
|---|---|
| Single-param boolean w/ parens (3 concepts) | 2,335 results, clean search box |
| Field codes | `title` 1,491; `sub` 4,065; `creator` 1,930 (observed, fluctuate) |
| Filters `peer_reviewed` + `articles` + `2018-2026` | Active filters: `2018-2026 \| Peer-reviewed Journals \| Articles` |
| AND-before-OR precedence | `zebrafish OR medaka AND cancer` = 91,564 vs `(zebrafish OR medaka) AND cancer` = 13,333 → parens mandatory |
| Chained-param pitfall | `query=any,contains,zebrafish,AND&query=any,contains,cancer` = 17 vs single-param = 27,802; 3-concept chained = 47,346,467 → chained format unreliable |
| Count fluctuation | `zebrafish` read 191,794 on one load, 91,327 on another → treat counts as estimates |

## 3. Forward-test scenarios (in-context, skill-faithful)

| # | Scenario | Expected | Observed | Status |
|---|---|---|---|---|
| 1 | Novice/vague ("paper on climate anxiety") | 2–4 questions, no query | 3 questions, zero query | PASS |
| 2 | Expert/complete spec | 0 questions + strategy confirmation + 4 formats | 0 questions; strategy line; 4 formats; URLs live-verified | PASS |
| 3 | Mid-detail + high-value gap (no material type) | Exactly 1 question | 1 question (articles vs books), then proceeds | PASS |
| 4 | Mid-detail, no high-value gap | Proceed, no filters + filtered variants | Proceeded; base URL live (16,214); variants provided | PASS |

Scenario 2 live evidence: filtered URL (peer-reviewed + articles + 2018–2026) returned
3,412 results with all three filters listed under "Active filters" and the clean boolean
expression in the search box. Scenario 4 base URL returned 16,214 results, clean box.

## 4. Findings and notes

- Primo display counts are estimates and fluctuate between loads; the skill always presents
  ranges and instructs result scanning rather than exact counts.
- When a missing detail's importance is ambiguous (e.g., material type), the skill errs
  toward a single clarifying question per the 95% confidence principle.
- The "Active filters" heading renders even with no filters applied; harmless UI behavior.
- Full encoding/syntax evidence lives in `references/primo-ve-syntax.md`; output layout in
  `references/response-template.md`.

## 5. Artifacts

- Skill directory: `/Users/d2worsham/Code/uc-library-search/`
- Files: `SKILL.md`, `agents/openai.yaml`, `references/primo-ve-syntax.md`,
  `references/response-template.md`, `scripts/build_url.py`

## 6. A-Z future-research integration (2026-09-25)

### Data pipeline

- Springshare API harvest: 805 assets → 667 visible, 138 hidden.
- Normalized metadata: 644 with subjects, 437 with types, 166 with featured subjects,
  294 featured subject associations.
- Subject-level `featured` values are numeric ranks (1–6), not binary; normalized as
  `featured_subjects` with rank preserved.
- HTML fully stripped from `description` and `more_info`.

### Integration

- Added future-research guidance to `SKILL.md` and a Future Research section to
  `references/response-template.md`.
- Added `scripts/recommend_databases.py` to rank A-Z candidates by featured subject match,
  subject match, type match, name/alt-name matches, and description overlap.
- The recommendation script excludes boolean operators and the `zz-test` test artifact.

### Forward-test scenario

**Input:** “Build a search strategy for social media and adolescent depression.”

**Query used:**
`("social media" OR Facebook) AND (anxiety OR depression) AND ("college students")`

**Filters:** peer-reviewed + articles only + 2018–2026.

**Generated UC Library Search URL:**
`https://search-library.ucsd.edu/discovery/search?query=any,contains,(%22social%20media%22%20OR%20Facebook)%20AND%20(anxiety%20OR%20depression)%20AND%20(%22college%20students%22)&tab=ArticleBooksEtc&search_scope=ArticlesBooksEtc&vid=01UCS_SDI:UCSD&mfacet=tlevel,include,peer_reviewed,1&mfacet=rtype,include,articles,1&facet=searchcreationdate,include,2018%7C,%7C2026,lk`

**Recommendation subjects used:** Psychology, Communication, Sociology.

**A-Z future-research candidates observed:**
1. Project Muse: Scholarly Journals and Books Online — Sociology featured subject, rank 1.
2. PsycINFO — Psychology featured subject, rank 1; strong subject coverage.
3. Social Sciences Full Text — Sociology featured subject, rank 1; strong subject coverage.
4. Sociological Abstracts — Sociology featured subject, rank 1; strong subject coverage.

### Status

- `quick_validate.py` → PASS.
- `build_url.py --self-test` → PASS (7 cases).
- `recommend_databases.py` → PASS on the forward-test scenario, returning 4 subject-aware
  candidates with rationale-bearing metadata.
- Future research section included after the primary UC Library Search strategy.

### Findings

- The recommendation helper performs well for subject-rich topics and keeps the large A-Z
  JSON out of context.
- Boolean operators were initially parsed as search terms; fixed by filtering `AND`, `OR`,
  and `NOT` from parsed query terms.
- `enable_popular` is currently empty for all assets, so the useful featured-subject signal is
  the numeric `featured` value in subject associations.
